<h1>Portfoilo Items </h1>

<!-- <a href="portfolios/new">Create New Portfolio Item</a><br> -->

<%= link_to "Create New Item", new_portfolio_path %> 


<% @portfolio_items.each do |portfolio_item| %>
<%# for the below line you can pass the portfolio_item without the .id and it will still work %>
<h3> <%= link_to portfolio_item.title, portfolio_path(portfolio_item.id) %></h3>
<p> <%= portfolio_item.subtitle %></p>
<p> <%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image  unless portfolio_item.thumb_image.nil? %>
<%= link_to "Edit", edit_portfolio_path(portfolio_item.id) %>
<%= link_to 'Delete Portfolio', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } %>

<!--
<a href="/portfolios/<%=portfolio_item.id%>/edit"> Edit also</a>
-->

<%#=  (double negative) image_tag portfolio_item.thumb_image  if !portfolio_item.thumb_image.nil? %>




<% end %>


<!-- difference between path and url using prefix from the #rake routes
<p>
<%= new_portfolio_path %> 
</p>

<p>
<%= new_portfolio_url %> 
</p>
-->